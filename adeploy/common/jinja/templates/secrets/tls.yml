apiVersion: v1
kind: Secret
metadata:
  name: {{ secret.get('name') }}
  {% if deployment.get('namespace', False) -%}
  namespace: {{ deployment.get('namespace') }}
  {%- endif %}
type: kubernetes.io/tls
data:
  tls.crt: {{ cert | base64_encode }}
  tls.key: {{ key | base64_encode }}